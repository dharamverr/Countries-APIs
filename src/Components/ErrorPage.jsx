import React from 'react'
import { useRouteError } from 'react-router'

export default function ErrorPage() {
  return (
    <div style={{fontSize : '3rem', textAlign: 'center', marginTop: '100px'}}>Route is not defined.</div>
  )
}
